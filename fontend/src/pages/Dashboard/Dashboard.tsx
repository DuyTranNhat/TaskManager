const Dashboard = ({ title }: { title: string }) => {
   return (
        <div>
            <h1>{title}</h1>
            <p>Đây là trang Dashboard</p>
        </div>
    );
}

export default Dashboard;   