const EditProfile = ({ title }: { title: string }) => {
    return (
        <div>
            <h1>{title}</h1>
            <p>Đây là trang Cài đặt hồ sơ</p>
        </div>
    );
};

export default EditProfile;
