const Register = ({ title }: { title: string }) => {
    return (
        <div>
            <h1>{title}</h1>
            <p>Đây là trang Đăng ký</p>
        </div>
    );
};

export default Register;
